apiVersion: v1
kind: Service
metadata:
  name: amazon-clone-app-service
  namespace: dev
spec:
  type: NodePort
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  selector:
    app: samazon-clone-app